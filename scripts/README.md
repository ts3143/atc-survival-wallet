# scripts

One-off / manually-run scripts, e.g. the M0 BTS On-Time Performance loader that
curates the flight pool and populates `flight_definitions` +
`flight_volatility_stats`.

Empty for now — this is scaffolding only.
