# Postgres connection string used by both the FastAPI app and Alembic migrations.
DATABASE_URL=postgresql://user:password@localhost:5432/atc_survival_wallet

# OpenSky Network (OAuth2 client credentials) — /states/all polling, 4,000 credits/day.
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=

# AeroDataBox — live/scheduled flight times, 600 units/month.
AERODATABOX_API_KEY=
